import clusterize from 'clusterize.js'

export default function renderTable (data, target) {
  console.log(data)
  // var clusterize = new Clusterize({
  //   rows: data,
  //   scrollId: `${target}Scroll`,
  //   contentId: `${target}Content`
  // });
}
